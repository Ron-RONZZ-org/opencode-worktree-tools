{
  "$schema": "https://opencode.ai/config.json",
  "plugin": [
    "~/.config/opencode/plugins/opencode-worktree-tools/index.ts"
  ],
  "instructions": [
    "Prefer worktree* plugin tools (worktreeCreate, worktreeDelete, worktreeList) over raw git worktree bash."
  ]
}
